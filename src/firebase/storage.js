// Firebase Storage requires the Blaze (paid) plan.
// Soulify uses base64 image encoding stored in Firestore instead — free, no setup needed.

export const uploadPostImage = null;  // not used
export const uploadAvatar = null;     // not used
export const deleteFile = null;       // not used
